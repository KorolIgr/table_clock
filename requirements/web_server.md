* By default, the web server returns the index page (the page template is described in `requirements\web\index.md`)
* Page files must be located in Littlefs
* The original html pages should be in the folder `data`
* The page should be compressed via gzip to save controller memory.
* The controller firmware must be performed together with the loading of current HTML pages

