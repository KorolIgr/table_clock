* Create a module to manage settings that are read from the eeprom when the controller boots.
* On command, the settings are saved in eeprom
* Create a structure to save WiFi settings for AP mode and STA mode, as well as to store selected LED pattern settings.
* Bind the settings to the new config for WiFi in AP mode with fallback to the settings specified by the constant in the code
* Bind the settings to the new config for WiFi in STA mode
* Link the settings of the selected pattern to the LED
* Remove the old code that specifically uses eeprom access and create centralized configuration management with a convenient structure. Change the configuration structure.
