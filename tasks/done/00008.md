* Remove fallback to HTML pages in the file `src\modules\wifi\WiFiManager.cpp`
* Create a script that, along with the firmware, will compress HTML pages using gzip and upload them to the controller's littleFS file system. To do this, you'll need to create a separate script and program it so it's called in the same action as the controller's firmware.