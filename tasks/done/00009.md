* implement the STA mode connection setting (see requirements in `requirements\wifi.md`)
* Implement control of the built-in LED in the controller and update the schematic in the file `hardware\scheme.md`
* implement mode indication via a built-in LED
* settings must be done through the web page (see requirements in `requirements\web\wifi_sta.md`)
* When you click the drop-down list, you should see the current list of available networks for connection.
* When you press the connect button, the data is saved in the eeprom and a connection is established to the selected network with the specified password.
* When you press the forget button, the data is erased from the eeprom and the device is disconnected from the network.
* When the controller boots, it should check for connection data in the eeprom, and if it is there, it should connect to the network automatically.
* When connecting, display the received IP address in the console.
